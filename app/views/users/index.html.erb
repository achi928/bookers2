<h1>ユーザーの一覧ページ</h1>

<h2>User info</h2>

<div>
  <%= image_tag current_user.get_profile_image(100,100) %>
  <p>name
    <%= current_user.name %>
  </p>
  <p>introduction
    <%= current_user.introduction %>
  </p>
  <% if @users == current_user %>
    <p><%= link_to "プロフィール編集のアイコン", edit_user_path(@user) %></p>
  <% end %>
</div>

<h1>New book</h1>

<%= form_with model: @book do |f| %>
  <label>Title</label>
    <%= f.text_field :title %>
  <label>Opinion</label>
    <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>


<h2>Users</h2>
<% @users.each do |user| %>
  <div>
    <p>imageあとでテーブルにする</p>
    <p>投稿ユーザー画像：<%= image_tag user.get_profile_image(100,100) %></p>
    <p>name<p>
    <p><%= user.name %><p>
    <p><%= link_to "Show" ,user_path(user.id) %></p>
  </div>
<% end %>
