<h1>Books#show</h1>
<p>Find me in app/views/books/show.html.erb</p>


<h1>New book</h1>

<%= form_with model: @book do |f| %>
  <label>Title</label>
    <%= f.text_field :title %>
  <label>Opinion</label>
    <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>


  <div>
    ユーザー画像にもリンク貼る
    <p>ユーザー画像設定する:<%= image_tag 'sample-author1.jpg' %></p>
    <p>Title テーブルで整える:<%= @book.title %></p>
    <p>Opinion:<%= @book.body %></p>
    <% if @book.user == current_user %>
      <%= link_to "destroy",book_path(@book),method: :delete %>
    <% end %>
  </div>